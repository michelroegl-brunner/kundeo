packages:
  - "apps/*"
  - "packages/*"

# Packages allowed to run postinstall build scripts (pnpm 11 gated by default).
allowBuilds:
  "@prisma/client": true
  "@prisma/engines": true
  prisma: true
  esbuild: true
  unrs-resolver: true
