name: Release

# Publishes the production Docker image to GHCR and the native release tarball
# as release artifacts.
#
# - Push to main: rebuild and push the image as :latest (+ :sha-<commit>), so
#   self-hosters tracking :latest always follow the tip of main. No tarball.
# - Version tag (v*) or published release: also tag the image :X.Y.Z / :X.Y for
#   pinning, and build + attach the native tarball to the release.
on:
  push:
    branches: ["main"]
    tags: ["v*"]
  release:
    types: [published]

permissions:
  contents: write # attach the tarball to the release
  packages: write # push the image to GHCR

concurrency:
  group: release-${{ github.ref }}
  cancel-in-progress: false

jobs:
  # ─── Docker image → GHCR ───────────────────────────────────────────────────
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository }}
          # e.g. v1.2.3 -> :1.2.3, :1.2, :latest
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest,enable={{is_default_branch}}
            type=sha

      - name: Build and push
        uses: docker/build-push-action@v6
        with:
          context: .
          # amd64 only: the arm64 leg cross-builds under QEMU, where Next 15's
          # native SWC build worker crashes with SIGILL intermittently. Our deploy
          # target is amd64; re-add arm64 via a native arm64 runner if ever needed.
          platforms: linux/amd64
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # ─── Native release tarball → release assets ───────────────────────────────
  # Only for tags / releases — a plain push to main just refreshes the image.
  tarball:
    if: startsWith(github.ref, 'refs/tags/') || github.event_name == 'release'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # tags, so build-tarball.sh can name the artifact

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Build release tarball
        env:
          DATABASE_URL: "postgresql://build:build@localhost:5432/build?schema=public"
          DIRECT_URL: "postgresql://build:build@localhost:5432/build?schema=public"
          BETTER_AUTH_SECRET: "ci-placeholder-secret-not-used-at-runtime"
          BETTER_AUTH_URL: "http://localhost:3000"
          KUNDEO_ENCRYPTION_KEY: "ci-placeholder-key-not-used-at-runtime"
          NEXT_TELEMETRY_DISABLED: "1"
        run: deploy/build-tarball.sh "${GITHUB_REF_NAME}"

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: kundeo-linux-tarball
          path: dist-release/*.tar.gz

      - name: Attach to release
        if: github.event_name == 'release'
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAG: ${{ github.event.release.tag_name }}
        run: gh release upload "$TAG" dist-release/*.tar.gz --clobber
